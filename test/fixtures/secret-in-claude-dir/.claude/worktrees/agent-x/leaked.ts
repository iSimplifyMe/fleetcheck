// Fixture: a secret inside a .claude agent-worktree copy.
// fleetcheck must NOT scan here — these are throwaway working copies that
// duplicate the real source and inflate finding counts.
const awsAccessKeyId = "AKIAZ7K2QX9WMNP4R3TB";
export { awsAccessKeyId };

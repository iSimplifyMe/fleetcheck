# fleetcheck

Fleet-wide linter for the iSimplifyMe Next.js/SST repo fleet. Encodes known
failure-mode rules and security checks so they fail in CI instead of in
production.

> Full README written in build phase 9. See `docs/DESIGN.md` for the design.
